file_client: local
master_type: disable

pillar_merge_lists: True

file_roots:
  base:
    - /srv/salt/base
  dev:
    - /srv/salt/dev
    - /srv/salt/base
  server:
    - /srv/salt/server
    - /srv/salt/dev
    - /srv/salt/base

# for the saltcheck purposes, the test will generate this pillar
ext_pillar:
    - cmd_yaml:
        command: "cat /tmp/pillar.json"

# after all changes nothing so commening out and re-using masterless.conf for travis host k8s provisining
providers:
  service: service

# hostname not respected?
id: salt-test-host

use_superseded:
  - module.run

env_order:
  - empty
  - base
  - gui
  - dev
  - server
