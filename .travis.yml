language: python

sudo: required

dist: trusty

services:
  - docker

env:
  - DOCKER_IMAGE=debian-sid
  - DOCKER_IMAGE=debian-jessie

before_install:
  - .travis/prepare.sh

script:
  - .travis/run.sh
