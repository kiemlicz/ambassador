ext_pillar:
  - git: []
  - puppet: /usr/bin/foreman-node
  - cmd_json: cat /var/cache/salt/master/file_ext/%s/gdrive_token
  - privgit:
    - main:
        env: base
        root: pillar
#  - kubectl:
#      config: "/tmp/admin.conf"
#      queries:
#        - kind: statefulsets
#          name: redis-cluster
#          key: "redis:kubernetes"
#        - kind: pod
#          selector: app=redis-cluster
#          key: "redis:kubernetes:pods"

# so that filesystem can override gitfs
ext_pillar_first: True

pillar_merge_lists: True
